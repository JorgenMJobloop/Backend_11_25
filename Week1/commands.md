# Starting a new C# console application

```sh
dotnet new console --use-program-main
``` 

# fetching changes from git repositories
```sh
git fetch # fetches the latest changes
git pull # pulls the latest remote changes to the local repository
```
